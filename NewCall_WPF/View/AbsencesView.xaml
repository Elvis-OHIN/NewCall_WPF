<UserControl x:Class="NewCall_WPF.View.AbsencesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NewCall_WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Name="btnEnregistrerSelection" Grid.Row="0" Content="Enregistrer Sélection" Click="btnEnregistrerSelection_Click"/>

        <ListView HorizontalAlignment="Center" Height="354" Grid.Row="1"  VerticalAlignment="Center" Width="576" Name="StudentList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Firstname" DisplayMemberBinding="{Binding firstname}" Width="120"/>
                    <GridViewColumn Header="Lastname" DisplayMemberBinding="{Binding lastname}" Width="120"/>
                    <GridViewColumn Header="Statut" DisplayMemberBinding="{Binding statut}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
